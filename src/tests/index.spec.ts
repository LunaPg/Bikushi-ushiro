import * as assert from 'assert';


describe('first', () => {
    it('should be a dummy test', () => {
        //expect('string').to.be.string;
        assert (true, 'Dummy test kill me !');
    });
});